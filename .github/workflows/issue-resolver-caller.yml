name: Auto-resolve issues

on:
  issues:
    types: [labeled]
  issue_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  auto-resolve:
    uses: ./.github/workflows/auto-resolve.yml
    with:
      trigger-label: "fix-me"
    secrets: inherit
